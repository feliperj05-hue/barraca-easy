# Barraca Easy — variaveis de ambiente (Vite)
#
# Copie este arquivo para `.env.local` e preencha com os valores reais.
# `.env.local` NAO e versionado (ver .gitignore).
#
# Somente variaveis com prefixo VITE_ sao expostas ao client
# via import.meta.env. A publishable key e publica por design
# (client-side, protegida por Row-Level Security no Supabase);
# ainda assim, mantenha-a fora do codigo, injetada por env.
#
# NUNCA coloque aqui a service_role key (secreta, server-side).

VITE_SUPABASE_URL=https://<seu-ref>.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxx

# --- Cobranca (SaaS, #90) --------------------------------------------
#
# Ambas OPCIONAIS. Sem elas o app nao quebra: a tela de barraca bloqueada
# apenas manda falar com o suporte pelo botao que ja existe.
#
# Sao publicas por natureza (chave Pix serve para receber, contato serve
# para ser achado). Mesmo assim ficam em env e nao no codigo, para nao
# depender de deploy novo quando mudarem.

# Chave Pix mostrada ao dono da barraca com pagamento em aberto.
VITE_PIX_CHAVE=

# Contato do suporte exibido na tela de bloqueio (telefone, e-mail ou @).
VITE_SUPORTE_CONTATO=

# --- Site comercial (#111, #114) --------------------------------------

# URL absoluta do site, usada em canonical e og:url. Sem ela, cai no host
# atual (barraca-easy.web.app). A virada de dominio e trocar SO esta linha.
VITE_SITE_URL=

# E-mail de contato publicado no site.
#
# ATENCAO: so ligue VITE_CONTATO_ATIVO=true depois que o dominio
# barracaeasy.com.br estiver registrado E a caixa de e-mail respondendo.
# Enquanto isso, mensagem mandada para esse endereco VOLTA, e o cliente fica
# achando que falou com a gente. Com a flag desligada, o site mostra o canal
# que funciona hoje (botao "Fale com o desenvolvedor", dentro do app).
VITE_CONTATO_EMAIL=contato@barracaeasy.com.br
VITE_CONTATO_ATIVO=false

